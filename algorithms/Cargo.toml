[package]
    name = "algorithms"
    version = "0.5.0"
    edition = "2021"

[dependencies]
    rand = "0.8.5"

[dev-dependencies]
    criterion = "0.5.1"

[[bench]]
    name = "my_benchmark"
    harness = false
